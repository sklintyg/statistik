ACTIVEMQ_BROKER_URL=${broker.amq.tcp.port}?jms.nonBlockingRedelivery=true&jms.redeliveryPolicy.maximumRedeliveries=3&jms.redeliveryPolicy.maximumRedeliveryDelay=6000&jms.redeliveryPolicy.initialRedeliveryDelay=4000&jms.redeliveryPolicy.useExponentialBackOff=true&jms.redeliveryPolicy.backOffMultiplier=2
ACTIVEMQ_RECEIVER_QUEUE_NAME=perf.certificate.queue
DB_NAME=${database.name}
DB_PORT=${mysql.service.port}
DB_SERVER=${mysql.service.host}
HSA_WS_CERTIFICATE_FILE=${application.dir}/certifikat/localhost.p12
HSA_WS_CERTIFICATE_TYPE=PKCS12
HSA_WS_TRUSTSTORE_FILE=${application.dir}/certifikat/truststore.jks
HSA_WS_TRUSTSTORE_TYPE=JKS
NTJP_WS_CERTIFICATE_FILE=${application.dir}/certifikat/localhost.p12
NTJP_WS_CERTIFICATE_TYPE=PKCS12
NTJP_WS_TRUSTSTORE_FILE=${application.dir}/certifikat/truststore.jks
REDIS_HOST=${redis.service.host}
REDIS_PORT=${redis.service.port}
SAML_KEYSTORE_ALIAS=localhost
SAML_KEYSTORE_FILE=file://${application.dir}/certifikat/localhost.p12
SCHEDULER_LOGJOB_CRON=0 0 3 29 2 ?
SPRING_PROFILES_ACTIVE=test,caching-enabled,testapi,hsa-stub,wc-hsa-stub,security-fake,ia-stub