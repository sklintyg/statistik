
# overrides default
spring.profiles.active=h2,dev,caching-enabled,testapi,hsa-stub,wc-hsa-stub,security-both,noprocessing,ia-stub

# H2 configuration
db.driver=org.h2.Driver
db.url=jdbc:h2:mem:dataSource;MODE=MySQL;IGNORECASE=TRUE
db.username=sa
db.password=
db.pool.maxSize=3

db.httpPort=8550
