---

- name: checkout config repo
  git: repo="{{ config_repository }}"
       dest="{{ config_folder }}"

- name: create etc folder link
  file: src="{{ config_etc_folder }}" dest="{{ tomcat_etc }}" state=link
